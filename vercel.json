{
  "build": {
    "outputDirectory": "dist",
    "env": {
      "NODE_ENV": "production"
    },
    "commands": {
      "build": "npm run build",
      "start": "npm run start"
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/dist/$1"
      }
    ]
  }
}
